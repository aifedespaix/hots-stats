node_modules/
.output/
.nuxt/
.data/
dist/
build/
*.log
.env
.env.*
!.env.example
.DS_Store
coverage/
*.tsbuildinfo

# Python daemon
daemon-python/.venv/
daemon-python/**/__pycache__/
daemon-python/*.spec
daemon-python/build/
daemon-python/dist/
daemon-python/**/*.egg-info/
daemon-python/.pytest_cache/
# CI and this project install deps via `pip install -e` from pyproject.toml
# (see build-daemon.yml) -- uv.lock is only ever a side effect of a
# contributor choosing to run tests locally via `uv run`, never read by
# anything in this repo.
daemon-python/uv.lock
